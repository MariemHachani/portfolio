
export default function portfolioLayout(
    { children }
        : {
            children: React.ReactNode,

        }) {
    return (
        <div>

            {children}

        </div>
    )
}